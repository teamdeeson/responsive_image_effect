name: Responsive image effect
description: Replaces Drupal image style url generator with a scale and crop image effect that can be specfied via URL arguments.
package: Media
core_version_requirement: ^8 || ^9

type: module
core: 8.x

dependencies:
  - drupal:image
  - drupal:focal_point
